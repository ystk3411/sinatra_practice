# sinatra_practice
以下の手順でアプリを立ち上げ

1.作業用ディレクトリでbundle init
2.bundle add sinatra --skip-installのコマンドを入力
3.bundle install --path vendor/bundleのコマンドを入力
4.bundle exec ruby app.rbコマンドを入力
5.http://localhost:4567へアクセスして動作を確認