# sinatra_practice
事前準備

* `postgres`の名称のデータベースを作成
* PostgreSQLを起動しておく

以下の手順でアプリを立ち上げ

1. 作業用ディレクトリで`git clone https://github.com/ystk3411/sinatra_practice`
2. `git clone`したディレクトへ移動
3. `bundle install`コマンドを入力
4. `bundle exec ruby myapp.rb`コマンドを入力
5. http://localhost:4567へアクセスして動作を確認
