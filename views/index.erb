<div>
  <a href="/memos/new" class="btn">
    追加
  </a>
  <ul>
    <% @memos.each do |memo| %>
      <li>
        <a href="/memos/<%= memo['id'] %>"><%= h(memo['title']) %></a>
      </li>
    <% end %>
  </ul>
</div>
